import { Helmet } from 'react-helmet';

const index = () => (
  <Helmet>
    <title>Noshit</title>
    <meta name={'description'} content={'Noshit application'} />
  </Helmet>
);

export default index;
